fastapi
uvicorn[standard]; sys_platform != 'win32' or platform_machine != 'ARM64'
uvicorn; sys_platform == 'win32' and platform_machine == 'ARM64'
psycopg[binary]; sys_platform != 'win32' or platform_machine != 'ARM64'
psycopg; sys_platform == 'win32' and platform_machine == 'ARM64'
sqlalchemy
pandas
openpyxl
reportlab
# Pillow : rognage du blanc autour du logo entreprise dans le devis
# (modules/ad_devis_api.py:_trim_logo_whitespace). Déjà tiré transitivement
# par reportlab pour l'embarquage PNG, explicité ici car on l'importe.
pillow
pyjwt
# python-multipart : requis par FastAPI pour File/Form (émission RELAIS du devis,
# POST multipart des octets PDF construits par le client — modules/ad_devis_api.py).
python-multipart
# PyMuPDF (fitz) : lecture/rendu PDF pour le harnais pré-push. REQUIS par
# tests/test_pdf_critical.py (page_count) ET tests/test_devis_fidelity.py
# (extraction texte + rendu image pour le diff jsPDF↔reportlab). Etait importe
# sans etre declare -> pre-push cassait dans un venv reconstruit. Corrige.
pymupdf
# Sprint C1 — client HTTP cross-service Ad BUD → Ad HUB (modules/hub_service.py).
# Aligné sur Ad VIU qui utilise déjà httpx>=0.27 pour son propre hub_service.
httpx>=0.27
